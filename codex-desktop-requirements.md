# Codex 桌面版应用需求分析文档

> 版本：v1.0  
> 日期：2026-08-25  
> 基于：OpenAI Codex CLI 开源源码 + Codex Desktop App 功能对标  
> 目标平台：Windows 10/11、macOS 12+

---

## 1. 项目概述

### 1.1 项目背景

OpenAI Codex 是一款以智能体（Agent）为核心的 AI 编程助手，目前以 CLI（命令行）形式开源，并推出了官方桌面版应用。本项目旨在**基于 Codex CLI 开源源码**，构建一款功能完整的跨平台桌面 GUI 应用，将 CLI 的全部能力以可视化、多窗口、可交互的方式呈现，同时保留与 CLI 的完全互操作性。

### 1.2 产品定位

- **不是**简单的 CLI 终端包装器，而是以 GUI 为一等公民的原生桌面应用
- **不是**ChatGPT 桌面端的复制，而是聚焦本地代码操作、终端执行、项目管理的编程智能体
- **核心价值**：将 Codex CLI 的 Agentic Coding 能力（读/写/改代码、执行命令、沙箱、MCP、Skills）通过多面板 GUI 释放，支持多项目并行、可视化 diff、内置终端、语音输入等桌面原生体验

### 1.3 设计原则

| 原则 | 说明 |
|------|------|
| **CLI 能力对等** | 桌面端必须覆盖 CLI 的全部功能，不做功能阉割 |
| **GUI 原生体验** | 充分利用桌面 OS 能力（多窗口、菜单栏、通知、拖拽、全局快捷键） |
| **配置互通** | 复用 `~/.codex/config.toml`、`AGENTS.md`、Skills、MCP 配置，与 CLI 零冲突共享 |
| **安全优先** | 沙箱、审批、权限模型在 GUI 中必须可见、可控、可审计 |
| **跨平台一致** | Windows 与 macOS 功能对齐，仅在 OS 原生交互细节上做适配 |

---

## 2. 目标与范围

### 2.1 功能目标

实现 Codex 桌面版的**全部核心功能**，包括但不限于：

1. 多项目 / 多会话并行管理
2. Agent 对话与代码操作（读、写、改、删）
3. 内置终端（Windows PowerShell / macOS zsh）
4. 代码 Diff 可视化与审查
5. Git 集成（分支、提交、diff、worktree）
6. 沙箱与审批策略可视化控制
7. MCP 协议支持（客户端 + 服务端）
8. Skills 系统（浏览、调用、管理）
9. 插件 / 连接器市场
10. 多模型配置与切换
11. 图像附件（截图、设计稿、线框图）
12. 语音输入 / 听写
13. 待办事项跟踪
14. 网页搜索工具
15. 计划模式（Plan Mode）
16. 会话历史管理（恢复、分叉、导出）
17. 上下文用量实时显示
18. 浏览器内置预览（Web 应用渲染测试）
19. 非交互 exec 模式（脚本 / CI 触发）
20. 自动化（Automations）与定时任务

### 2.2 非目标（Out of Scope）

- 移动端（iOS / Android）支持
- Web 版（浏览器中运行）
- 团队协作 / 多人实时编辑（v1 不做，预留架构）
- 自研大模型（仅做模型接入层，不训练模型）
- IDE 插件（VS Code / Cursor 扩展由官方维护，本项目不重复造轮子，但可做深度联动）

### 2.3 平台范围

| 平台 | 最低版本 | 架构 |
|------|----------|------|
| Windows | 10 22H2 / 11 | x64、ARM64 |
| macOS | 12 Monterey | Intel x64、Apple Silicon (arm64) |

---

## 3. 用户角色与典型场景

### 3.1 用户角色

| 角色 | 特征 | 核心诉求 |
|------|------|----------|
| **独立开发者** | 单人项目，全栈开发 | 快速生成代码、调试、多项目切换 |
| **团队开发者** | 企业项目，有规范约束 | AGENTS.md 合规、代码审查、Git 工作流 |
| **技术负责人** | 架构决策、代码质量把控 | 批量审查、架构分析、自动化任务 |
| **初学者 / 非专业用户** | 编程经验有限 | 引导式交互、可视化操作、低门槛 |
| **运维 / DevOps** | 脚本自动化、CI/CD | exec 非交互模式、定时任务、MCP 连接器 |

### 3.2 典型场景

**场景 A：多项目并行开发**
> 开发者同时打开 3 个项目（前端、后端、脚本），每个项目独立会话，侧边栏切换，内置终端各自独立，Agent 在各项目中分别操作文件。

**场景 B：大型重构 + 计划模式**
> 开发者输入"重构用户认证模块"，开启 Plan Mode，Codex 生成分步计划 → 用户审批 → 逐步执行 → 每步可查看 diff → 全部完成后 /review 统一审查。

**场景 C：设计稿驱动开发**
> 开发者拖拽一张 UI 设计稿截图到对话窗口，Codex 识别设计 → 生成前端代码 → 内置浏览器预览 → 迭代调整。

**场景 D：代码审查自动化**
> 开发者在 Git 仓库中执行 /review --base main，Codex 对比分支差异 → 输出审查报告 → 标记问题文件 → 可直接跳转修复。

**场景 E：非交互批量任务**
> 开发者通过 Automations 配置：每天凌晨 2 点对指定仓库执行 `codex exec "检查并修复 lint 错误"`，结果通过系统通知推送。

---

## 4. 功能需求详细规格

### 4.1 应用外壳与导航

#### FR-1.1 主窗口布局

采用**可配置的多栏布局**，默认三栏：

```
┌──────────┬──────────────────────┬──────────────────┐
│          │                      │                  │
│  项目/    │   对话 / 工作区      │   多功能面板     │
│  会话侧   │                      │  (可切换)        │
│  边栏     │                      │  - Diff 查看     │
│          │                      │  - 文件树        │
│          │                      │  - 终端          │
│          │                      │  - 浏览器预览    │
│          │                      │  - MCP 状态      │
└──────────┴──────────────────────┴──────────────────┘
```

- 左侧栏宽度可拖拽调整，可折叠
- 右侧多功能面板支持 Tab 切换，可独立弹出为浮动窗口
- 中间对话区支持**分屏**（左右并排两个会话）
- 所有面板支持拖拽重排、保存为布局预设

#### FR-1.2 左侧边栏

包含以下区域（自上而下）：

1. **应用 Logo + 全局菜单按钮**
2. **项目列表**（Project Sidebar）
   - 显示最近打开的项目（文件夹 / Git 仓库）
   - 每个项目显示：图标、名称、路径、Git 分支名、活跃会话数
   - 支持拖拽文件夹到侧边栏添加项目
   - 支持项目分组 / 收藏
3. **会话列表**（Thread List）
   - 当前项目下的所有 Agent 会话
   - 显示：会话标题、最后活跃时间、状态（进行中 / 已完成 / 等待审批）
   - 支持新建、重命名、删除、分叉（Fork）、恢复（Resume）
4. **底部状态栏**
   - 当前模型名称
   - 沙箱模式指示
   - 审批策略指示
   - 设置入口

#### FR-1.3 顶部工具栏

- 当前项目路径显示（可点击复制 / 在文件管理器中打开）
- Git 分支选择器（下拉切换分支 / 创建分支 / worktree 管理）
- 模型切换器（下拉选择模型，显示推理强度）
- 沙箱模式快速切换（只读 / 工作区写入 / 完全访问）
- 审批策略快速切换
- 全局搜索（搜索会话内容 / 代码 / 文件）
- 布局预设切换

#### FR-1.4 菜单栏（OS 原生）

**macOS 菜单栏：**
- Codex（关于、偏好设置、退出）
- 文件（新建会话、打开项目、关闭窗口、导出会话）
- 编辑（撤销、重做、剪切、复制、粘贴、查找）
- 视图（切换侧栏、切换面板、全屏、缩放）
- 项目（Git 操作、运行命令、安装依赖）
- 帮助（文档、快捷键、检查更新）

**Windows 菜单栏：** 功能对等，融入应用窗口顶部或系统菜单。

#### FR-1.5 全局快捷键

| 快捷键 | 功能 |
|--------|------|
| `Cmd/Ctrl + N` | 新建会话 |
| `Cmd/Ctrl + T` | 新建标签页 |
| `Cmd/Ctrl + W` | 关闭当前标签 |
| `Cmd/Ctrl + Shift + F` | 全局搜索 |
| `Cmd/Ctrl + ,` | 打开设置 |
| `Cmd/Ctrl + K` | 模型切换 |
| `Cmd/Ctrl + L` | 聚焦输入框 |
| `Cmd/Ctrl + Shift + P` | 命令面板（Command Palette） |
| `Cmd/Ctrl + /` | 切换 Plan Mode |
| `Cmd/Ctrl + Shift + D` | 打开 Diff 面板 |
| `Cmd/Ctrl + ` ` | 切换内置终端 |
| `Cmd/Ctrl + Shift + M` | 切换 MCP 面板 |
| `Esc` | 取消当前 Agent 操作 / 关闭弹窗 |

---

### 4.2 Agent 对话系统

#### FR-2.1 对话输入区

位于中间工作区底部，包含：

1. **多行文本输入框**
   - 支持 Markdown 语法高亮
   - 支持 `@` 提及文件 / 函数 / 类（自动补全）
   - 支持 `#` 引用会话 / 项目
   - 支持 `/` 斜杠命令（完整 45+ 命令，见 FR-2.6）
   - 支持 `$` 引用连接器 / App（如 `$github`）
   - 自动高度扩展，最大高度占窗口 40%

2. **附件按钮**
   - 图片附件（截图、设计稿、线框图）→ 支持拖拽 / 粘贴 / 文件选择
   - 文件附件（任意文件，作为上下文注入）
   - 文件夹附件（整个目录作为上下文）
   - AppShot（屏幕区域截图，一键截取并附加）

3. **功能开关**
   - Plan Mode 开关（计划模式：先出计划再执行）
   - 语音输入按钮（按住说话 / 切换听写模式）
   - 完全计算机访问授权开关（临时提升权限）

4. **模型选择器**
   - 当前会话模型切换（不影响全局默认）
   - 推理强度调节（minimal / low / medium / high / xhigh）
   - 温度 / Top-P 等高级参数（折叠面板）

5. **发送按钮**
   - `Enter` 发送，`Shift+Enter` 换行
   - 发送时显示当前上下文用量预估

#### FR-2.2 对话消息渲染

- **用户消息**：右侧气泡，显示文本 + 附件缩略图
- **Agent 消息**：左侧，支持以下内容块的流式渲染：
  - 纯文本 / Markdown（代码块带语言高亮 + 复制按钮）
  - 工具调用卡片（显示工具名、参数、执行状态、结果摘要）
  - 文件操作卡片（读 / 写 / 改 / 删，显示文件路径 + diff 预览）
  - 命令执行卡片（显示命令、stdout、stderr、退出码）
  - 待办事项列表（可勾选、可展开子任务）
  - 搜索结果卡片（网页搜索结果，带标题 + 链接 + 摘要）
  - 图片生成 / 识别结果
- **流式输出**：Agent 回复逐字流式渲染，工具调用实时显示状态
- **消息操作**：每条消息支持复制、重新生成、编辑（修改用户消息后重跑）、删除

#### FR-2.3 工具调用可视化

Agent 调用工具时，在对话流中插入**工具调用卡片**，状态流转：

```
[待执行] → [执行中] → [成功] / [失败] / [等待审批]
```

- 卡片显示：工具图标、工具名称、调用参数（可折叠）、执行耗时
- 文件操作类工具：卡片内嵌 mini diff，点击展开完整 diff
- 命令执行类工具：卡片内嵌终端输出，可展开完整日志
- 等待审批的工具：卡片高亮，显示"批准 / 拒绝 / 批准全部"按钮
- 支持按工具类型筛选显示（如只看文件修改、只看命令执行）

#### FR-2.4 待办事项跟踪

- Agent 在处理复杂任务时自动生成待办列表
- 待办列表显示在对话流中，也可固定到右侧面板
- 每个待办项：标题、状态（待办 / 进行中 / 已完成 / 已跳过）、关联的文件 / 命令
- 用户可手动添加、编辑、删除、重新排序待办项
- 待办完成进度实时更新

#### FR-2.5 上下文用量显示

- 输入框右上角显示**上下文用量环形指示器**
- 显示：已用 tokens / 总上下文窗口、百分比
- 颜色分级：绿色（< 50%）、黄色（50%-80%）、红色（> 80%）
- 悬停显示详细：系统提示、历史消息、工具结果、附件各占多少
- 支持手动触发**上下文压缩**（Compact），压缩后显示摘要

#### FR-2.6 斜杠命令（Slash Commands）

完整支持 Codex CLI 的 45+ 斜杠命令，在 GUI 中通过输入框 `/` 触发自动补全面板：

| 分类 | 命令 | 说明 |
|------|------|------|
| **会话管理** | `/new` | 新建会话 |
| | `/clear` | 清空当前会话 |
| | `/fork` | 分叉当前会话 |
| | `/resume` | 恢复历史会话 |
| | `/compact` | 压缩上下文 |
| | `/status` | 显示当前会话状态 |
| **代码操作** | `/review` | 代码审查（支持 --base / --uncommitted / --commit） |
| | `/diff` | 显示当前所有未提交修改 |
| | `/apply` | 应用待审修改 |
| | `/revert` | 撤销修改 |
| **模式切换** | `/plan-mode` | 切换计划模式 |
| | `/permissions` | 设置审批策略 |
| | `/sandbox` | 设置沙箱模式 |
| | `/model` | 切换模型 |
| | `/personality` | 设置回复风格（friendly / pragmatic / none） |
| **上下文** | `/mention` | 附加文件到对话 |
| | `/init` | 生成 AGENTS.md 骨架 |
| | `/ignore` | 管理 .codexignore |
| **扩展** | `/mcp` | 列出 MCP 工具及状态 |
| | `/skills` | 浏览并调用 Skills |
| | `/apps` | 浏览连接器 / App |
| | `/plugins` | 浏览已安装插件和市场 |
| | `/agent` | 切换并行子 Agent 线程 |
| **其他** | `/help` | 显示帮助 |
| | `/shortcut` | 显示快捷键 |
| | `/config` | 打开配置文件 |
| | `/logout` | 登出 |

> 完整命令列表以 Codex CLI 最新版本为准，GUI 需保持同步更新机制。

---

### 4.3 项目与文件管理

#### FR-3.1 项目管理

- **打开项目**：通过菜单 / 拖拽 / 命令面板打开本地文件夹
- **项目识别**：自动检测 Git 仓库、package.json、pyproject.toml 等项目配置
- **项目设置**：每个项目独立保存
  - 项目级 `AGENTS.md`（自动检测 / 可编辑）
  - 项目级 `.codexignore`（忽略文件规则）
  - 项目级模型覆盖、沙箱设置、审批策略
  - 项目级 MCP 配置、Skills 启用列表
- **最近项目**：记住最近 20 个项目，支持搜索
- **项目分组**：可创建分组（如"工作"、"个人"、"开源"）

#### FR-3.2 文件树面板

右侧多功能面板的"文件"Tab：

- 显示当前项目的文件树
- **修改高亮**：被 Agent 修改过的文件标记颜色（新增=绿色、修改=黄色、删除=红色）
- **未保存指示**：Agent 修改但未写入磁盘的文件标记
- 文件操作：右键菜单（打开、重命名、删除、复制路径、在终端中打开）
- 支持文件拖拽到对话区作为附件
- 支持 `.codexignore` 规则的文件灰显

#### FR-3.3 文件查看与编辑

- 点击文件树中的文件 → 在中间区域打开**文件预览标签页**
- 支持语法高亮（100+ 语言）
- Agent 修改的文件 → 显示**内联 diff**（左侧旧代码灰色，右侧新代码高亮）
- 支持**三向 diff 视图**：原始版本 / Agent 修改版本 / 合并版本
- 用户可在 diff 视图中逐块接受 / 拒绝修改
- 支持直接在预览中编辑文件（轻量编辑器，非完整 IDE）
- 大文件（> 1MB）自动截断并提示

#### FR-3.4 AGENTS.md 集成

- 项目根目录检测到 `AGENTS.md` 时自动加载为系统提示
- 侧边栏显示 AGENTS.md 状态指示器（已加载 / 未找到）
- 内置 AGENTS.md 编辑器（带模板 / 最佳实践提示）
- 支持 `/init` 命令自动生成 AGENTS.md 骨架
- AGENTS.md 修改后即时生效（无需重启会话）

---

### 4.4 内置终端

#### FR-4.1 终端面板

右侧多功能面板的"终端"Tab，或底部可拉起的终端面板：

- **Windows**：默认 PowerShell 7+，可切换 cmd / WSL / Git Bash
- **macOS**：默认 zsh，可切换 bash / fish
- 支持**多终端标签页**，每个项目独立终端会话
- 完整 PTY 支持（vim、htop、npm 等交互式程序正常运行）
- 支持复制 / 粘贴 / 全选 / 搜索
- 支持字体大小调节、主题切换（跟随应用主题）
- 终端输出可拖拽选中后"发送给 Codex"（作为上下文）

#### FR-4.2 Agent 与终端交互

- Agent 执行的命令在终端面板中**实时显示**（可配置是否在独立终端中运行）
- Agent 命令执行时，终端面板自动聚焦并滚动
- 支持**审批拦截**：Agent 执行命令前，根据审批策略弹出确认
- 用户可在终端中手动输入命令，与 Agent 操作互不干扰
- 终端历史与 Agent 命令历史关联，可回溯

#### FR-4.3 终端配置

- 每个项目可配置默认 Shell、启动命令、工作目录
- 支持环境变量注入（项目级 `.env` 文件自动加载）
- 支持终端会话持久化（关闭应用后重开恢复终端状态）

---

### 4.5 Git 集成

#### FR-5.1 Git 状态与分支

- 顶部工具栏显示当前分支，点击展开分支管理面板
- 分支列表：本地分支、远程分支、最近使用分支
- 分支操作：切换、创建、删除、重命名、合并、变基
- **Worktree 支持**：
  - 列出所有 worktree
  - 创建新 worktree（指定分支 + 目录）
  - 在 worktree 间快速切换（每个 worktree 独立项目窗口）
  - 删除 worktree

#### FR-5.2 Diff 与变更管理

- **Diff 面板**（右侧多功能面板"Diff"Tab）：
  - 显示所有未提交变更（已暂存 / 未暂存）
  - 文件列表 + 每个文件的 diff 预览
  - 支持统一 diff / 分栏 diff 切换
  - 支持逐块暂存 / 取消暂存 / 丢弃
  - 支持行内评论（给特定 diff 行添加备注）
- Agent 产生的修改自动进入未暂存区，用户审查后决定是否提交

#### FR-5.3 提交与历史

- 提交面板：填写 commit message（支持 Agent 生成 commit message）、选择变更、提交
- 历史面板：显示提交历史，点击查看 commit diff
- 支持 `git push` / `git pull` / `git fetch`（带进度显示）
- 支持 PR 预览（检测到远程 PR 时显示链接）

#### FR-5.4 代码审查（/review）

- 支持三种审查模式：
  1. `--base <branch>`：对比目标分支（PR 风格审查）
  2. `--uncommitted`：审查未提交变更
  3. `--commit <hash>`：审查特定提交
- 审查结果以**结构化报告**呈现：
  - 问题分级：严重 / 警告 / 建议 / 好评
  - 每个问题关联具体文件 + 行号，点击跳转
  - 支持自定义审查规则（通过 `code_review.md` 或 AGENTS.md 配置）
- 审查报告可导出为 Markdown / JSON

---

### 4.6 沙箱与安全模型

#### FR-6.1 沙箱模式

可视化三档沙箱切换，顶部工具栏 + 设置页均可配置：

| 模式 | 文件系统 | 网络 | 说明 |
|------|----------|------|------|
| **read-only**（默认） | 只读，不可修改任何文件 | 禁止出站网络（除 API 调用） | 最安全，适合代码阅读 / 解释 |
| **workspace-write** | 仅可修改当前项目目录内文件 | 允许网络（可配置域名白名单） | 平衡安全与功能，日常开发推荐 |
| **danger-full-access** | 全文件系统读写 |  unrestricted 网络 | 关闭沙箱，高风险，需二次确认 |

- 沙箱模式在 UI 中**始终可见**（顶部状态栏颜色标识）
- 切换到 `danger-full-access` 时弹出警告对话框，需输入确认
- 每个项目可独立配置默认沙箱模式
- 沙箱违规操作被拦截时，显示明确的错误提示 + 申诉路径

#### FR-6.2 审批策略

五种审批模式，控制 Agent 执行操作前是否需要用户确认：

| 策略 | 文件操作 | 命令执行 | 网络访问 | MCP 工具 |
|------|----------|----------|----------|----------|
| **never** | 自动执行 | 自动执行 | 自动执行 | 自动执行 |
| **on-request**（默认） | Agent 请求时审批 | Agent 请求时审批 | Agent 请求时审批 | Agent 请求时审批 |
| **on-dangerous-command** | 自动执行 | 危险命令需审批 | 自动执行 | 自动执行 |
| **before-completion** | 自动执行 | 自动执行 | 自动执行 | 自动执行，全部完成后统一审批 |
| **always** | 全部需审批 | 全部需审批 | 全部需审批 | 全部需审批 |

- **审批弹窗**：需要审批时，对话流中插入审批卡片 + 系统通知
  - 显示：操作类型、目标文件 / 命令、风险评估
  - 按钮：批准（仅本次）、批准全部（本次会话后续同类操作自动批准）、拒绝、编辑后执行
- **审批规则自定义**：支持通过 `execpolicy` 规则文件定义细粒度审批规则
  - 按命令模式匹配（如 `rm -rf *` 始终拒绝）
  - 按文件路径匹配（如 `~/.ssh/*` 始终审批）
  - 按网络域名匹配
- 审批历史可审计（记录所有审批决策，支持导出）

#### FR-6.3 危险操作防护

- **命令风险评估**：Agent 执行命令前自动评估风险等级
  - 高危命令（`rm -rf /`、`mkfs`、`dd`、修改系统文件等）→ 强制审批 + 红色警告
  - 中危命令（`npm install`、`git push --force`、修改环境变量）→ 提示审批
  - 低危命令（`ls`、`cat`、`git status`）→ 自动执行
- **文件删除保护**：Agent 删除文件前，默认移入应用回收站（可恢复），而非直接永久删除
- **网络访问控制**：workspace-write 模式下，网络访问默认走域名白名单（可配置）
- **密钥泄露检测**：Agent 输出中检测到 API Key / 密码 / Token 时自动遮罩

---

### 4.7 模型配置

#### FR-7.1 模型管理

- **模型列表**：内置 OpenAI 模型目录（gpt-5-codex、gpt-5、o4、o4-mini 等），自动同步最新模型
- **自定义模型**：支持添加兼容 OpenAI API 格式的第三方模型
  - 配置项：模型名称、API Base URL、API Key、上下文窗口大小、是否支持视觉、是否支持工具调用
- **模型供应商**：支持多供应商（OpenAI、Azure OpenAI、本地 vLLM / Ollama 等）
- **模型测试**：添加模型后可发送测试消息验证连通性

#### FR-7.2 模型参数

全局默认 + 会话级覆盖：

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `model` | 模型名称 | gpt-5-codex |
| `model_reasoning_effort` | 推理强度 | medium |
| `model_verbosity` | 输出详细度 | medium |
| `temperature` | 温度 | 模型默认 |
| `top_p` | Top-P | 模型默认 |
| `max_tokens` | 最大输出 tokens | 模型默认 |

#### FR-7.3 配置文件互通

- 完全复用 `~/.codex/config.toml` 作为配置源
- GUI 设置页修改后同步写入 config.toml
- 支持 **Profiles**：多套配置快速切换（通过 `--profile` 或 GUI 切换器）
- 配置优先级（从低到高）：内置默认 → 用户全局 config.toml → 项目级 config.toml → 会话级覆盖 → 命令行参数
- GUI 中显示当前生效配置的来源层级

---

### 4.8 MCP（Model Context Protocol）

#### FR-8.1 MCP 客户端

- **MCP 服务器管理**：设置页中添加 / 编辑 / 删除 MCP Server
  - 配置项：名称、命令（stdio）或 URL（SSE）、环境变量、参数
  - 支持从 MCP 目录一键安装流行 MCP Server
- **MCP 工具列表**：右侧面板"MCP"Tab 显示所有已连接 MCP Server 及其工具
  - 每个工具：名称、描述、参数 schema、调用状态
  - 支持手动调用 MCP 工具（测试用）
  - 支持启用 / 禁用特定工具
- **MCP 状态监控**：显示每个 MCP Server 的连接状态、启动耗时、最近调用
- MCP 工具被 Agent 调用时，在对话流中显示为工具调用卡片（与内置工具一致）

#### FR-8.2 MCP 服务端

- 支持将 Codex 本身作为 MCP Server 运行（`codex mcp-server`）
- GUI 中可配置：是否启用 MCP 服务端、传输方式（stdio / SSE）、监听端口
- 可被其他 Agent / 应用消费（如在 Claude Code 中调用 Codex）

---

### 4.9 Skills 系统

#### FR-9.1 Skills 浏览与调用

- **Skills 面板**：通过 `/skills` 或右侧面板打开
  - 分类展示：官方 Skills、已安装社区 Skills、项目本地 Skills
  - 每个 Skill：名称、描述、适用场景、触发关键词
  - 支持搜索 / 筛选
- **调用方式**：
  - 自动触发：Agent 根据对话内容自动匹配并加载 Skill
  - 手动调用：`/skills <name>` 或点击 Skill 卡片
- **Skill 内容**：Skill 加载后，其指令注入系统提示，影响 Agent 行为

#### FR-9.2 Skills 管理

- **安装**：从 Skills 市场浏览 / 安装社区 Skills
- **创建**：内置 Skill 创建向导（生成 SKILL.md 模板 + 目录结构）
- **编辑**：本地 Skill 可直接在 GUI 中编辑
- **项目级 Skills**：项目目录下 `.codex/skills/` 自动加载
- **用户级 Skills**：`~/.codex/skills/` 全局可用

---

### 4.10 插件与连接器

#### FR-10.1 插件系统

桌面端支持五种插件组件，一个插件可包含一项或多项：

| 类型 | 说明 |
|------|------|
| **Skills** | 特定工作类型的可复用指令，按需加载 |
| **连接器（Connectors）** | 连接 GitHub、Slack、Google Drive、Linear 等外部工具，可读取并执行操作 |
| **MCP Servers** | 连接器背后的服务层，定义工具、处理认证、返回结构化数据 |
| **主题（Themes）** | 自定义 UI 外观（颜色、字体、图标） |
| **扩展面板（Panels）** | 向右侧多功能面板注册自定义 Tab |

#### FR-10.2 插件市场

- 内置插件市场浏览器：浏览、搜索、安装、更新、卸载
- 插件详情页：描述、截图、评分、版本、作者、源码链接
- 插件权限声明：安装时显示所需权限（文件访问、网络、MCP 等），用户确认后安装
- 自动更新检查（可配置）

#### FR-10.3 连接器认证

- OAuth 认证流程：内置浏览器完成 OAuth，Token 安全存储
- API Key 认证：加密存储于系统钥匙串（macOS Keychain / Windows Credential Manager）
- 连接器状态：显示已连接 / 未连接 / 过期，支持重新认证
- 已连接的连接器可通过 `$app-slug` 在对话中引用

---

### 4.11 多模态支持

#### FR-11.1 图像附件

- **输入方式**：
  - 拖拽图片文件到对话区
  - 剪贴板粘贴（截图后直接 Ctrl/Cmd+V）
  - 点击附件按钮选择文件
  - AppShot：内置屏幕截图工具（选区 / 窗口 / 全屏）
- **支持格式**：PNG、JPG、GIF、WebP、SVG
- **图像预览**：附件显示缩略图，点击放大查看
- **多图支持**：一次可附加多张图片
- **图像理解**：Agent 可识别截图中的代码、UI 设计、错误信息、图表等
- **使用场景**：设计稿转代码、错误截图调试、图表数据分析、UI 还原

#### FR-11.2 语音输入

- **语音听写**：输入框麦克风按钮，按住说话或切换连续听写模式
- 支持中文、英文及多语言识别
- 语音转文字实时显示在输入框，可编辑后发送
- **语音对话**（可选高级模式）：支持实时语音交互，Agent 回复可语音播报
- 系统级语音快捷键：全局热键触发语音输入（即使应用在后台）

---

### 4.12 浏览器内置预览

#### FR-12.1 内置浏览器

右侧多功能面板"浏览器"Tab，基于系统 WebView（macOS WKWebView / Windows WebView2）：

- Agent 生成 Web 应用后，可一键在内置浏览器中预览
- 支持本地文件路径 / localhost URL 加载
- 浏览器工具栏：前进 / 后退 / 刷新 / 地址栏 / 外部打开
- 支持开发者工具（右键检查元素）
- **截图反馈**：浏览器中截图可一键发送回对话区，形成"生成→预览→截图→迭代"闭环
- 支持响应式模式切换（桌面 / 平板 / 手机视口）

---

### 4.13 会话历史与管理

#### FR-13.1 会话存储

- 所有会话自动持久化到本地（SQLite 数据库）
- 会话内容：消息历史、工具调用记录、文件修改记录、配置快照
- 支持会话导出为 JSON / Markdown
- 支持会话导入

#### FR-13.2 会话操作

| 操作 | 说明 |
|------|------|
| **新建** | 创建空白会话 |
| **重命名** | 修改会话标题（支持 Agent 自动生成标题） |
| **删除** | 移入回收站，可恢复 / 永久删除 |
| **恢复（Resume）** | 恢复历史会话，继续对话（保留完整上下文） |
| **分叉（Fork）** | 从当前会话某个时间点创建分支，尝试不同方案 |
| **归档** | 归档不活跃会话，不显示在主列表但可搜索 |
| **收藏** | 标记重要会话 |
| **搜索** | 全文搜索会话内容（消息、文件名、代码片段） |

#### FR-13.3 会话时间线

- 每个会话显示时间线视图：按时间顺序展示所有关键事件
  - 用户消息、Agent 回复、工具调用、文件修改、审批决策
- 支持跳转到任意时间点（查看当时的完整状态）
- 支持从任意时间点 Fork

---

### 4.14 非交互模式（exec）

#### FR-14.1 exec 命令

支持通过 GUI 触发非交互模式，或通过命令行调用：

```bash
codex exec "修复所有 lint 错误" --full-auto
codex exec -i design.png "实现这个 UI" --json
codex exec review --base main
```

- GUI 中：命令面板 → "Run exec command" → 输入指令 → 选择输出格式
- 输出格式：stdout 纯文本 / JSONL（结构化）/ 写入文件
- 支持 `--resume` 恢复历史 exec 会话
- 支持 `--full-auto` 全自动模式（跳过所有审批，需沙箱配合）

#### FR-14.2 自动化（Automations）

- 可视化自动化规则编辑器：
  - 触发器：定时（cron）、文件变更、Git 事件、手动触发
  - 条件：项目匹配、分支匹配、文件路径匹配
  - 动作：执行 `codex exec` 指令
- 示例自动化：
  - 每天凌晨 2 点：对 main 分支执行代码审查
  - 保存 `.py` 文件时：自动运行 lint 修复
  - 新建 PR 时：自动生成审查报告
- 自动化执行日志：记录每次触发时间、输入、输出、状态
- 执行完成后系统通知推送结果

---

### 4.15 计划模式（Plan Mode）

#### FR-15.1 计划生成与执行

- 开启 Plan Mode 后，Agent 不直接执行操作，而是先生成**分步计划**
- 计划以可交互的待办列表呈现：
  - 每个步骤：标题、描述、预计操作类型、关联文件
  - 步骤可展开查看详细子计划
- 用户操作：
  - **全部批准**：按顺序执行所有步骤
  - **逐步批准**：执行一步后暂停，等待下一步批准
  - **编辑计划**：修改步骤内容 / 顺序 / 删除步骤
  - **拒绝**：取消计划
- 执行过程中每步完成后显示结果摘要 + diff
- 全部完成后可触发 `/review` 统一审查

---

### 4.16 网页搜索

#### FR-16.1 搜索工具

- Agent 可自动调用网页搜索工具获取实时信息
- 搜索结果在对话流中显示为卡片：标题 + URL + 摘要 + 来源
- 支持手动触发搜索（输入框 `!search <query>`）
- 搜索配置：
  - 启用 / 禁用搜索
  - 搜索结果数量（默认 5）
  - 搜索地域 / 语言偏好
- 搜索到的网页内容可被 Agent 读取并引用（自动附带来源链接）

---

### 4.17 子 Agent 并行

#### FR-17.1 多 Agent 线程

- 支持在一个会话中启动多个**子 Agent** 并行处理不同子任务
- 子 Agent 管理面板：显示所有活跃子 Agent 及其状态
- `/agent` 命令切换当前查看的子 Agent 线程
- 子 Agent 间可共享上下文（通过主 Agent 协调）
- 使用场景：
  - 前端 + 后端同时开发
  - 多个独立文件并行修改
  - 研究 + 编码并行进行
- 每个子 Agent 独立显示工具调用、文件操作、待办列表

---

### 4.18 设置与偏好

#### FR-18.1 设置页

分类设置页，搜索可定位：

| 分类 | 内容 |
|------|------|
| **通用** | 语言、主题（明/暗/跟随系统）、字体、启动行为、更新通道 |
| **模型** | 默认模型、推理强度、API Key、自定义模型、供应商 |
| **安全** | 沙箱默认模式、审批策略、危险命令规则、文件删除策略、密钥遮罩 |
| **MCP** | MCP Server 管理、MCP 服务端配置 |
| **Skills** | 已安装 Skills、市场入口、本地 Skills 路径 |
| **插件** | 已安装插件、市场、插件权限 |
| **终端** | 默认 Shell、字体、主题、环境变量 |
| **Git** | 用户信息、默认分支名、diff 工具配置 |
| **快捷键** | 所有快捷键查看与自定义 |
| **通知** | 系统通知开关、声音、勿扰时段 |
| **高级** | 配置文件路径、日志级别、调试模式、数据导出 / 清除 |

#### FR-18.2 配置文件

- 主配置：`~/.codex/config.toml`（与 CLI 共享）
- 项目配置：`<project>/.codex/config.toml`
- 会话配置：存储在 SQLite 中
- GUI 设置修改后实时写入对应配置文件
- 支持配置导入 / 导出（JSON 格式）

---

### 4.19 系统集成

#### FR-19.1 系统通知

- Agent 完成长任务、需要审批、发生错误时推送系统通知
- macOS：原生 Notification Center
- Windows：原生 Toast 通知
- 通知可点击：点击后聚焦应用并跳转到对应会话
- 可配置通知类型（审批、完成、错误、自动化结果）

#### FR-19.2 全局快捷键

- 可配置全局热键（应用在后台也可触发）：
  - 快速唤起 Codex 窗口
  - 触发语音输入
  - 截图并发送到 Codex（AppShot）
- 全局热键在设置中可自定义

#### FR-19.3 右键菜单扩展

- macOS Finder / Windows 资源管理器右键菜单：
  - "用 Codex 打开"（文件夹 / 文件）
  - "发送到 Codex"（文件作为附件）
- 需要安装时注册 Shell 扩展（可选项）

#### FR-19.4 URL Scheme

- 注册 `codex://` URL Scheme：
  - `codex://open?path=/path/to/project` → 打开项目
  - `codex://new?prompt=...` → 新建会话并发送指令
  - `codex://resume?id=...` → 恢复指定会话
- 支持从浏览器 / 其他应用唤起

#### FR-19.5 命令行启动

- 安装时将 `codex` 命令加入 PATH（与 CLI 共享）
- 支持 `codex .` 在当前目录打开桌面应用
- 支持 `codex --gui` 强制启动 GUI 模式
- 桌面应用与 CLI 共享配置、会话、历史

---

## 5. 非功能需求

### 5.1 性能

| 指标 | 要求 |
|------|------|
| 应用冷启动 | < 3 秒（SSD），< 5 秒（HDD） |
| 会话切换 | < 200ms |
| 文件树渲染 | 10,000 文件项目 < 500ms（虚拟滚动） |
| 对话流式渲染 | 60fps，无卡顿 |
| Diff 渲染 | 10,000 行 diff < 1 秒 |
| 内存占用 | 空闲 < 300MB，正常使用 < 800MB |
| 大文件处理 | 支持 10MB 以下文件流畅查看 |

### 5.2 可靠性

- 会话数据零丢失：每次消息写入即持久化（WAL）
- 崩溃恢复：应用异常退出后重启，自动恢复未完成会话
- Agent 操作原子性：文件修改支持回滚
- 网络异常：API 调用失败自动重试（指数退避），不丢失上下文
- 自动更新：更新失败自动回滚到上一版本

### 5.3 安全性

- API Key / Token 加密存储于系统钥匙串，不明文写入配置文件
- 沙箱隔离：Agent 进程在沙箱中运行，文件 / 网络访问受控
- 审批审计：所有审批决策记录日志，不可篡改
- 遥测数据：可选匿名使用统计，默认关闭，用户可随时退出
- 无数据外传：除用户配置的 API 端点外，不向任何第三方服务器发送代码 / 会话数据

### 5.4 可访问性

- 支持键盘全操作（无鼠标也可完成所有功能）
- 屏幕阅读器支持（macOS VoiceOver / Windows Narrator）
- 高对比度主题
- 字体大小可调节（12px - 24px）
- 颜色不单独作为信息传递手段（配合图标 / 文字）

### 5.5 国际化

- v1 支持：简体中文、英文
- 架构预留多语言支持，社区可贡献翻译
- 日期 / 时间 / 数字格式跟随系统区域设置

### 5.6 可扩展性

- 插件 API 稳定（v1 后不做破坏性变更）
- MCP 协议完全兼容官方规范
- 主题系统支持 CSS 变量覆盖
- 自定义面板 API 允许插件向 UI 注入 React 组件

---

## 6. 技术架构建议

### 6.1 技术栈选型

| 层级 | 技术选型 | 理由 |
|------|----------|------|
| **桌面框架** | **Tauri 2.x**（首选）或 Electron | Tauri：Rust 后端 + Web 前端，体积小（~10MB）、内存低、性能好、安全性高；Electron：生态成熟、开发快，但体积大（~150MB）、内存高 |
| **前端框架** | React 18 + TypeScript + Vite | 生态成熟，组件库丰富，开发效率高 |
| **状态管理** | Zustand（轻量）+ React Query（服务端状态） | 避免 Redux 复杂度，适合桌面应用 |
| **UI 组件库** | shadcn/ui + Tailwind CSS | 可定制、轻量、现代化 |
| **代码编辑器** | CodeMirror 6 / Monaco Editor | 语法高亮、diff 渲染、代码编辑 |
| **终端仿真** | xterm.js + node-pty（Tauri 下用 portable-pty） | 业界标准 PTY 方案 |
| **本地数据库** | SQLite（通过 rusqlite / better-sqlite3） | 会话存储、缓存、索引 |
| **Codex CLI 集成** | 内嵌 Codex CLI 二进制 + stdio JSON-RPC 通信 | 复用官方源码，不重写 Agent 逻辑 |
| **WebView** | macOS WKWebView / Windows WebView2 | 内置浏览器预览 |
| **打包分发** | Tauri Bundler（dmg / msi / exe）+ 自动更新 | 原生安装包，支持签名和自动更新 |

### 6.2 架构分层

```
┌─────────────────────────────────────────────────────┐
│                   表现层 (Frontend)                   │
│  React UI · 对话面板 · 文件树 · Diff · 终端 · 设置    │
└──────────────────────┬──────────────────────────────┘
                       │ IPC (Tauri command / event)
┌──────────────────────▼──────────────────────────────┐
│                  应用服务层 (Rust Backend)            │
│  会话管理 · 项目管理 · 配置管理 · 插件系统 · 通知      │
│  终端管理 · Git 操作 · 沙箱控制 · 审批引擎 · 加密存储  │
└──────────────────────┬──────────────────────────────┘
                       │ stdio / JSON-RPC
┌──────────────────────▼──────────────────────────────┐
│              Codex CLI 核心 (内嵌二进制)               │
│  Agent 循环 · 工具调用 · 模型通信 · MCP · Skills      │
│  沙箱执行 · 审批策略 · 上下文管理 · 对话压缩            │
└─────────────────────────────────────────────────────┘
```

### 6.3 关键设计决策

1. **内嵌 Codex CLI 而非重写**：通过 stdio 以 JSON-RPC 方式与 Codex CLI 通信，最大化复用官方 Agent 逻辑，降低维护成本，保持与 CLI 功能同步
2. **Rust 后端做桌面能力层**：文件系统、终端、Git、通知、加密、沙箱等 OS 级操作由 Rust 实现，性能好且安全
3. **前端纯渲染**：React 只负责 UI 渲染和用户交互，不直接操作文件系统 / 终端，所有操作通过 IPC 走后端
4. **会话状态双写**：前端状态（Zustand）+ 后端 SQLite 持久化，操作即写，崩溃可恢复

---

## 7. 跨平台适配

### 7.1 Windows 适配

| 项目 | 适配内容 |
|------|----------|
| **Shell** | 默认 PowerShell 7，回退 Windows PowerShell 5.1，支持 cmd / WSL2 / Git Bash |
| **路径** | 支持 UNC 路径、长路径（> 260 字符）、反斜杠自动转换 |
| **文件权限** | Windows ACL 兼容，沙箱基于工作目录限制 |
| **终端** | ConPTY 支持，兼容 Windows 控制台程序 |
| **通知** | Windows Toast 通知（AppUserModelID 注册） |
| **快捷键** | 全局热键通过 RegisterHotKey API |
| **安装包** | MSI + NSIS exe，支持企业级静默安装 |
| **签名** | Microsoft Authenticode 代码签名 |
| **WebView2** | 检测并引导安装 WebView2 Runtime（Win10 可能未预装） |

### 7.2 macOS 适配

| 项目 | 适配内容 |
|------|----------|
| **Shell** | 默认 zsh（macOS 10.15+），支持 bash / fish |
| **架构** | Universal Binary（Intel x64 + Apple Silicon arm64） |
| **终端** | 原生 PTY，支持 tmux / screen |
| **通知** | Notification Center，支持通知声音 / 交互按钮 |
| **快捷键** | 全局热键通过 Carbon RegisterEventHotKey / MASShortcut |
| **菜单栏** | 原生 macOS 菜单栏 + 可选状态栏图标（Menu Bar Extra） |
| **触摸板** | 支持手势（双指滚动、捏合缩放、三指拖拽） |
| **Touch Bar** | 可选支持（上下文相关操作） |
| **安装包** | DMG + pkg，支持拖入安装 |
| **签名** | Apple Developer ID 签名 + 公证（Notarization） |
| **沙箱** | 可选 App Sandbox（若上架 Mac App Store） |
| **权限** | 首次访问桌面 / 文档 / 下载目录时请求 TCC 权限 |

### 7.3 跨平台差异处理策略

- **功能对齐**：核心功能（对话、文件、Git、MCP、Skills）在两平台完全一致
- **OS 原生体验**：菜单栏、通知、快捷键、文件对话框等遵循各平台 HIG（Human Interface Guidelines）
- **配置隔离**：平台相关配置（Shell 路径、WebView 设置等）独立存储，不跨平台同步
- **条件编译**：Rust 后端通过 `#[cfg(target_os)]` 处理平台差异，前端通过 `process.platform` 适配

---

## 8. 数据存储

### 8.1 存储结构

```
~/.codex/                              # 全局配置目录（与 CLI 共享）
├── config.toml                        # 主配置文件
├── AGENTS.md                          # 全局 AGENTS.md（可选）
├── skills/                            # 用户级 Skills
├── mcp-servers.json                   # MCP Server 配置
└── plugins/                           # 已安装插件

~/Library/Application Support/Codex/   # macOS 应用数据
%APPDATA%\Codex\                       # Windows 应用数据
├── sessions.db                        # SQLite：会话、消息、工具调用
├── cache/                             # 缓存（模型目录、缩略图、搜索索引）
├── logs/                              # 应用日志
├── themes/                            # 用户自定义主题
└── automations.json                   # 自动化规则配置
```

### 8.2 数据模型（核心表）

**sessions 表**
| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT (UUID) | 会话 ID |
| project_path | TEXT | 项目路径 |
| title | TEXT | 会话标题 |
| model | TEXT | 使用的模型 |
| sandbox_mode | TEXT | 沙箱模式 |
| approval_policy | TEXT | 审批策略 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 最后活跃时间 |
| status | TEXT | 状态（active / archived / deleted） |
| config_snapshot | JSON | 会话配置快照 |

**messages 表**
| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT (UUID) | 消息 ID |
| session_id | TEXT | 关联会话 |
| role | TEXT | user / assistant / system / tool |
| content | TEXT | 消息内容（Markdown / JSON） |
| tool_calls | JSON | 工具调用列表 |
| tool_call_id | TEXT | 工具结果关联 ID |
| created_at | DATETIME | 时间戳 |
| token_usage | JSON | tokens 用量 |

**file_changes 表**
| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT (UUID) | 变更 ID |
| session_id | TEXT | 关联会话 |
| message_id | TEXT | 关联消息 |
| file_path | TEXT | 文件路径 |
| operation | TEXT | create / modify / delete |
| diff | TEXT | unified diff |
| applied | BOOLEAN | 是否已写入磁盘 |
| created_at | DATETIME | 时间戳 |

**approvals 表**
| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT (UUID) | 审批 ID |
| session_id | TEXT | 关联会话 |
| operation_type | TEXT | file_write / command / network / mcp |
| operation_detail | JSON | 操作详情 |
| decision | TEXT | approved / rejected / pending |
| decided_at | DATETIME | 决策时间 |

---

## 9. 安全与隐私

### 9.1 凭据管理

- API Key、OAuth Token、连接器密钥统一存储于系统钥匙串
  - macOS：Keychain Services
  - Windows：Credential Manager / DPAPI
- 配置文件中仅存储密钥引用 ID，不存储明文
- 导出配置时密钥默认排除（需显式勾选并输入主密码）

### 9.2 沙箱实现

- **文件系统沙箱**：通过 Rust 后端拦截所有文件操作，根据当前沙箱模式和工作目录判断是否允许
- **网络沙箱**：Agent 发起的网络请求通过代理层，按域名白名单 / 沙箱模式过滤
- **进程隔离**：Codex CLI 子进程以受限权限运行，不继承父进程全部权限
- **workspace-write 模式**：仅允许写入项目目录及其子目录，项目目录外只读

### 9.3 隐私保护

- 代码和会话数据仅存储于本地，不上传任何服务器（除用户配置的模型 API）
- 遥测数据匿名化，不含代码内容 / 文件路径 / 会话文本
- 支持一键清除所有本地数据
- 支持离线模式（使用本地模型时完全断网可用）

---

## 10. 分发与更新

### 10.1 分发渠道

| 渠道 | 平台 | 说明 |
|------|------|------|
| 官网下载 | Win + Mac | 主要渠道，dmg / msi / exe |
| Microsoft Store | Windows | 可选，需 MSIX 打包 |
| Mac App Store | macOS | 可选，需 App Sandbox 适配 |
| Homebrew Cask | macOS | `brew install --cask codex` |
| Scoop / Chocolatey | Windows | 命令行安装 |
| GitHub Releases | 全平台 | 开源版本发布 |

### 10.2 自动更新

- 内置更新检查器：启动时 + 每日定时检查
- 更新通道：Stable（稳定）/ Beta（测试）/ Nightly（每日构建）
- 增量更新：二进制 diff，减少下载量
- 更新失败自动回滚
- 企业环境支持禁用自动更新（通过配置 / 组策略）

---

## 11. 里程碑与优先级

### 11.1 MVP（v0.1 - 核心可用）

**目标**：单平台（macOS）核心对话 + 文件操作可用

- [ ] 项目打开 / 会话管理
- [ ] Agent 对话（流式输出、工具调用卡片）
- [ ] 文件读写改（基于 Codex CLI stdio）
- [ ] 基础 Diff 查看
- [ ] 沙箱模式切换（只读 / 工作区写入）
- [ ] 审批策略（on-request）
- [ ] 配置文件读写（config.toml）
- [ ] 内置终端（基础 PTY）

### 11.2 Beta（v0.5 - 功能完整）

**目标**：双平台功能对齐，核心功能全部可用

- [ ] Windows 平台适配
- [ ] Git 集成（分支、diff、提交、worktree）
- [ ] 代码审查（/review）
- [ ] MCP 客户端 + 服务端
- [ ] Skills 系统
- [ ] 图像附件
- [ ] 计划模式
- [ ] 会话历史（恢复、分叉、搜索）
- [ ] 上下文用量显示
- [ ] 网页搜索
- [ ] 插件系统基础
- [ ] 系统通知
- [ ] 自动更新

### 11.3 v1.0（正式发布）

**目标**：生产级质量，全功能上线

- [ ] 语音输入
- [ ] 内置浏览器预览
- [ ] 插件市场
- [ ] 连接器（GitHub / Slack 等）
- [ ] 自动化（Automations）
- [ ] 子 Agent 并行
- [ ] 多语言（中文 + 英文）
- [ ] 可访问性
- [ ] 性能优化（大项目、大文件）
- [ ] 崩溃恢复 + 数据完整性保障
- [ ] 代码签名 + 公证
- [ ] 全渠道分发

### 11.4 v1.x（后续迭代）

- [ ] 团队协作（共享会话、评论）
- [ ] IDE 深度联动（VS Code / Cursor 扩展互通）
- [ ] 更多语言支持
- [ ] 企业版（SSO、审计日志、集中管理）
- [ ] AI 模型微调（基于用户代码风格）

---

## 12. 风险与应对

| 风险 | 影响 | 概率 | 应对策略 |
|------|------|------|----------|
| Codex CLI 接口变动 | 高 | 中 | 封装适配层，CLI 版本锁定 + 定期同步更新；JSON-RPC 协议版本检测 |
| Tauri 2.x 生态不成熟 | 中 | 低 | 关键功能有 Rust crate 替代方案；备选 Electron 迁移路径 |
| 沙箱逃逸漏洞 | 高 | 低 | 安全审计、渗透测试、漏洞赏金；沙箱实现最小权限原则 |
| 大项目性能问题 | 中 | 高 | 虚拟滚动、懒加载、索引优化、文件监听节流 |
| 跨平台一致性 bug | 中 | 中 | CI 双平台测试、端到端测试覆盖、平台特定 issue 标签 |
| 模型 API 成本过高 | 中 | 中 | 支持本地模型、上下文压缩、token 用量提示、预算告警 |
| 用户数据丢失 | 高 | 低 | WAL 持久化、自动备份、崩溃恢复、数据导出功能 |

---

## 附录 A：Codex CLI 内置工具清单

Codex CLI 内置以下工具，桌面端需全部支持并可视化：

| 工具 | 说明 |
|------|------|
| `read_file` | 读取文件内容 |
| `write_file` | 写入文件（新建 / 覆盖） |
| `edit_file` | 编辑文件（字符串替换 / 多光标） |
| `delete_file` | 删除文件 |
| `list_directory` | 列出目录内容 |
| `search_files` | 文件名搜索（glob） |
| `grep` | 内容搜索（正则） |
| `run_command` | 执行终端命令 |
| `web_search` | 网页搜索 |
| `web_fetch` | 读取网页内容 |
| `check_status` | 获取会话状态 / 上下文用量 |
| `todo_write` | 管理待办事项 |
| `tool_search` | 搜索可用工具（含 MCP） |
| `mcp_call` | 调用 MCP 工具 |

---

## 附录 B：配置文件参考（config.toml）

```toml
# ~/.codex/config.toml

# ── 模型 ──────────────────────────────
model = "gpt-5-codex"
model_provider = "openai"
model_reasoning_effort = "medium"   # minimal | low | medium | high | xhigh
model_verbosity = "medium"

# ── 安全边界 ──────────────────────────
sandbox_mode = "workspace-write"    # read-only | workspace-write | danger-full-access
approval_policy = "on-request"       # never | on-request | on-dangerous-command | before-completion | always

# ── 网络 ──────────────────────────────
network_enabled = true
network_domain_whitelist = ["github.com", "api.openai.com", "npmjs.org"]

# ── 外观 ──────────────────────────────
personality = "pragmatic"            # friendly | pragmatic | none
theme = "dark"                        # light | dark | system

# ── 工具 ──────────────────────────────
[tools.web_search]
enabled = true
max_results = 5

[tools.web_fetch]
enabled = true

# ── MCP ───────────────────────────────
[mcp_servers.github]
command = "npx"
args = ["-y", "@modelcontextprotocol/server-github"]
env = { GITHUB_PERSONAL_ACCESS_TOKEN = "***" }

# ── Profiles ───────────────────────────
[profile.review]
model = "gpt-5-codex"
approval_policy = "never"
sandbox_mode = "read-only"

[profile.fullauto]
approval_policy = "never"
sandbox_mode = "workspace-write"
```

---

*文档结束*
